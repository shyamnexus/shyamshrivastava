"use client";

import { motion } from "framer-motion";
import { Button } from "@/components/ui/button";

export default function Hero() {
  return (
    <section className="relative overflow-hidden">
      <div className="mx-auto flex min-h-[90vh] max-w-7xl items-center px-6">

        <div className="max-w-3xl">

          <motion.p
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            className="mb-4 text-blue-400 uppercase tracking-[0.3em]"
          >
            Engineering Leader
          </motion.p>

          <motion.h1
            initial={{ opacity: 0, y: 30 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: .2 }}
            className="text-6xl font-extrabold leading-tight"
          >
            Building Intelligent
            <br />
            Embedded Products
            <br />
            That Scale.
          </motion.h1>

          <motion.p
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: .4 }}
            className="mt-8 max-w-2xl text-lg leading-8 text-slate-400"
          >
            I lead cross-functional engineering teams to build secure,
            production-ready embedded systems across surveillance,
            industrial automation, medical devices and IoT —
            from architecture and firmware to manufacturing
            and certification.
          </motion.p>

          <div className="mt-10 flex gap-4">
            <Button size="lg">
              Explore Projects
            </Button>

            <Button
              size="lg"
              variant="outline"
            >
              Download Resume
            </Button>
          </div>

          <div className="mt-20 grid grid-cols-2 gap-8 md:grid-cols-4">

            <Metric
              value="20+"
              label="Years"
            />

            <Metric
              value="40+"
              label="Products"
            />

            <Metric
              value="12+"
              label="Engineers"
            />

            <Metric
              value="4"
              label="Domains"
            />

          </div>

        </div>

      </div>
    </section>
  );
}

function Metric({
  value,
  label,
}: {
  value: string;
  label: string;
}) {
  return (
    <div>
      <div className="text-3xl font-bold text-blue-500">
        {value}
      </div>

      <div className="mt-2 text-slate-500">
        {label}
      </div>
    </div>
  );
}